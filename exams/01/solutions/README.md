# Exam 01 Solutions

This workspace is intentionally split by output type:

- `derivations/`: written mathematics and structured solution notes.
- `code/`: Python scripts, notebooks, or helpers used to verify the mathematics.
- `artifacts/`: figures, tables, and exported outputs.

The directories are currently placeholders so the first exam can be solved without changing the repository layout again.
