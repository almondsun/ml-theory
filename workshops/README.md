# Workshops

Workshop material is organized one workshop per directory.

Each workshop directory can contain:

- the main notebook,
- short contextual notes,
- helper code or local assets if the workshop grows beyond a single notebook.

This avoids returning to a flat notebook dump as the course accumulates material.
